# 공공데이터포털(data.go.kr) 일반 인증키 — "디코딩된" 키를 넣는다.
# KCI 논문 검색(/research) 화면에서만 쓴다. 없어도 나머지 화면은 전부 동작한다.
VITE_DATAGO_SERVICE_KEY=

# 오픈API 호출 경로. 개발 서버 프록시를 쓰므로 비워두면 된다.
# 운영에서 별도 게이트웨이를 둔다면 그 주소를 넣는다.
VITE_OPENAPI_BASE_URL=
